# impl-with-commit

Agent skill: implement code and create one git commit per completed task, with terse Conventional Commits messages.

## Install

```bash
npx skills add hsbyeon1/impl-with-commit [-g]
```

## Activate

Attach or invoke the skill (`/impl-with-commit`, or ask to "commit as you go" while implementing).
